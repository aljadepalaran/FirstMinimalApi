# First Minimal API
This is a minimal API written in C#. It makes use of dependency injection to configure endpoints. It uses InMemory database for simplicity.

## Features
- Registration
- Login
- Fetch all users

## Important Notes
- There is no validation on any fields
- Password hashing is insecure
- Not all methods are asynchronous